// PT-домен: Form (соответствует домену PrimeVue)
// Здесь маппятся слоты компонентов формы на утилиты (Tailwind или .ui-*)
// Важно: глобальные ключи компонентов указываются в нижнем регистре.

const form = {
  // InputText — согласно скриншоту доступен только root
  inputtext: {
    // delta-only: layout управляется враппером `.app-input` и FloatLabel,
    // skin приходит из пресета/SCSS на данный момент. Здесь без дубликатов.
    root: { class: '' },
  },
  // Остальные компоненты домена будут добавляться позже (только root на первом этапе).
}

export default form
